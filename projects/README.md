# User proof workspace

Create one subdirectory here for each EasyCrypt project, and keep that
project's target file and project-owned `.ec`/`.eca` dependencies together in
that directory. See [the root README](../README.md#prove-your-first-lemma) for
the supported `$prove` and `/prove` workflows.
