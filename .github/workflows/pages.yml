# Deploys the project homepage at / and the benchmark browser at /results/,
# together with the curated public run bundles read by the browser.
name: pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build-deploy:
    if: github.repository == 'SkyShannonProver/shannon-prover'
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4

      - name: Assemble static site
        run: |
          mkdir -p site/results
          cp website/index.html site/index.html
          cp bundle_browser/index.html site/results/index.html
          python3 bundle_browser/build_manifest.py --public --out site/results/manifest.json
          python3 bundle_browser/build_manifest.py --public --timeline-js --out site/results/manifest.js
          cp -r agent_view_runs site/agent_view_runs
          rm -f site/agent_view_runs/INDEX.md
          touch site/.nojekyll
          echo "site size: $(du -sh site | cut -f1)"

      - uses: actions/configure-pages@v5

      - uses: actions/upload-pages-artifact@v3
        with:
          path: site

      - id: deployment
        uses: actions/deploy-pages@v4
