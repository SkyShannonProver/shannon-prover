"""Product-mandated no-model gates. The research experiment scaffolding
that used to live here is now the top-level ``experiments`` package;
``proof_terminal_outcome_sentinel`` stays because AGENTS.md requires it
before any live run after terminal/session/tree changes."""
