"""One EasyCrypt proof session's on-disk state and views: the session file
state machine and runtime, structured events, tactic execution / preflight
artifacts, projections and workspace views, and the compiler input surface.
The backend CLI entry stays at ``core/easycrypt/session_cli.py``."""
